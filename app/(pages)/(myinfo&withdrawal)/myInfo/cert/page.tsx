import UserCert from "@/components/organisms/UserCert";

//TODO: template 또는 page(component)로 만들어서 재사용.

const cert = () => {
  const active = "";
  return (
    <>
      <UserCert />
    </>
  );
};

export default cert;
